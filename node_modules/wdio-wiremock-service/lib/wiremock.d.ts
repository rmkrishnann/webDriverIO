export declare class WireMock {
    static download(from: string, to: string): Promise<void | Error>;
    private static writeData;
    private static logDownloadMessage;
    private static httpRequest;
}
