declare class CleanupTotal {
    private _errorCount;
    private _cleanupList;
    constructor();
    /**
     * @deprecated Don't use this method if *wdio-cleanuptotal-service* is enabled.
     */
    initialize(): void;
    /**
     *
     * @param cleanupFunction Insert a cleanup function to the cleanup stack.
     */
    addCleanup(cleanupFunction: Function): void;
    /**
     * @deprecated Don't use this method if *wdio-cleanuptotal-service* is enabled.
     */
    finalize(serviceOptions: {
        customLoggerMethod: Function;
    }): Promise<void>;
    private printToLog;
}
declare const _default: CleanupTotal;
export default _default;
